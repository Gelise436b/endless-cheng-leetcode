### 单题精析：LeetCode [3] 无重复字符的最长子串（Medium）

---

#### 🔍 **题目核心**
给定字符串 `s`，找出其中**不含重复字符的最长子串**的长度。  
→ 典型 **不定长滑动窗口（Variable-size Sliding Window）** 问题。

---

#### 🧠 **解题思想**
- **窗口定义**：`[left, right]` 表示当前考察的子串；
- **扩张**：`right` 指针不断右移，将新字符纳入窗口；
- **收缩条件**：当新字符 `s[right]` 已存在于窗口中 → 窗口非法（有重复）；
- **收缩方式**：持续从左侧弹出字符（`left++`），直到窗口内不再包含 `s[right]`；
- **更新答案**：每次窗口合法后，用 `right - left + 1` 更新最大长度。

> ✅ **关键洞察**：  
> “遇到重复字符” ≠ “重置整个窗口”，而是**精准收缩至重复字符的下一个位置**。  
> 滑窗天然保证了这一点——通过 `while (window contains c)` 循环左移。

---

#### 💡 **两种实现对比**

| 方案 | 数据结构 | 原理 | 优劣 |
|------|--------|------|------|
| **哈希表计数** | `unordered_map<char, int>` | 记录每个字符频次，`cnt[c] > 1` 判重 | 通用性强，适用于需频次统计的变体（如“最多 K 次重复”） |
| **哈希集合** | `unordered_set<char>` | 仅记录存在性，`count(c) > 0` 判重 | **本题更简洁高效**，因只需判断“是否重复”，无需频次 |

> 📌 **本题推荐使用 `unordered_set`**：逻辑清晰、代码简短、性能略优。

---

#### ✅ **最终代码（Set 实现）**
```cpp
class Solution {
public:
    int lengthOfLongestSubstring(string s) {
        int n = s.length(), ans = 0, left = 0;
        unordered_set<char> window;
        for (int right = 0; right < n; ++right) {
            char c = s[right];
            // 收缩窗口直到 c 不再重复
            while (window.count(c)) {
                window.erase(s[left]);
                left++;
            }
            window.insert(c);                     // 插入当前字符
            ans = max(ans, right - left + 1);     // 更新最大长度
        }
        return ans;
    }
};
```

---

#### ⚙️ **复杂度分析**
- **时间复杂度**：`O(n)`  
  每个字符最多被 `right` 访问一次、被 `left` 弹出一次，双指针各走 `n` 步；
- **空间复杂度**：`O(min(m, n))`  
  `m` 为字符集大小（ASCII 为 128），最坏情况窗口包含所有不重复字符。

---

#### 🧩 **延伸思考**
- 若题目变为“**最多允许 k 个重复字符**”，则必须使用 **频次哈希表**；
- 若字符集极小（如仅小写字母），可用 `bool seen[26]` 数组替代哈希结构，进一步提速；
- 此题是**不定长滑窗的“Hello World”**，务必做到：
  - 5 分钟内手写无 bug；
  - 能口述窗口扩张/收缩的触发逻辑；
  - 能类比到 [904] 水果成篮、[1493] 最多一个 0 等变体。

> ✨ **一句话总结**：  
> **“右扩无脑进，左缩精准退，合法即更新。”**
